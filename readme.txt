// Este repo es para analizar la modularización de nuestra app de backend
Para comenzar a probar este contenido, primero instale las dependencias mediante:
    npm install

Luego ejecute el siguiente comando para crear el source de código JS:
    npm run tsc

Para probar la app en modo development, proceda a:
    npm run dev